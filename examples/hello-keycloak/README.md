# Vue 2 + Vue Router - check-sso

In this example the application has a secret route `/secret` which is only accessible
if you are authenticated. The regular `/` home view can be accessed without being
authenticated
